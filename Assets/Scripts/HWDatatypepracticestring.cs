using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HWDatatypepracticestring : MonoBehaviour
{
    public string rupaul = "she done already done had herses";

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(rupaul);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
